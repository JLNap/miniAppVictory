# 🎯 Telegram Mini App — Викторина

Мини-приложение для Telegram с викторинами по разным категориям: общие знания, наука, история.

## Что внутри

- **Главный экран** — выбор категории викторины
- **Викторина** — вопросы с 4 вариантами ответа, прогресс, подсчёт очков
- **Результаты** — итог и процент правильных ответов; результат можно отправить боту через `sendData`
- Поддержка **тёмной и светлой** темы Telegram
- Адаптивная вёрстка под мобильные устройства

## Как запустить локально

1. Поднять любой статический сервер из папки проекта, например:

   ```bash
   cd telegram-quiz-miniapp
   npx serve .
   ```

2. Открыть в браузере (для теста без Telegram):  
   `http://localhost:3000`  
   Mini App будет работать, но без полной интеграции с Telegram (например, без темы и `sendData`).

## Как подключить к боту в Telegram

1. Создай бота в [@BotFather](https://t.me/BotFather): команда `/newbot`.
2. В BotFather выбери бота → **Bot Settings** → **Menu Button** или **Configure Mini App** (в зависимости от пунктов меню).
3. Укажи URL твоего Mini App:
   - для продакшена — HTTPS-адрес, например `https://твой-домен.com`
   - для теста можно использовать туннель: [ngrok](https://ngrok.com), [localtunnel](https://localtunnel.github.io/www/) и т.п.
4. В коде бота (на сервере) можно обрабатывать данные из Mini App: при завершении викторины вызывается `tg.sendData()` с JSON вида:
   ```json
   {
     "type": "quiz_result",
     "category": "general",
     "score": 4,
     "total": 5,
     "percent": 80
   }
   ```
   Бот получит эти данные в апдейте (например, в `message.web_app_data`).

## Добавление своих вопросов

В файле **`js/quiz-data.js`**:

- Добавь новую категорию в объект `QUIZ_DATA` по аналогии с `general`, `science`, `history`.
- У каждого вопроса укажи `question`, массив `answers` и индекс правильного ответа `correct` (0–3).

## Деплой на Vercel

1. **Залить код на GitHub**
   - Репозиторий: [github.com/JLNap/miniAppVictory](https://github.com/JLNap/miniAppVictory).
   - В папке проекта в терминале:
   ```bash
   cd /Users/jlnap/myProjects/telegram-quiz-miniapp
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/JLNap/miniAppVictory.git
   git push -u origin main
   ```

2. **Подключить репозиторий к Vercel**
   - Зайди на [vercel.com](https://vercel.com) и войди в аккаунт.
   - **Add New…** → **Project**.
   - **Import** репозитория с GitHub (если нужно — дай Vercel доступ к GitHub).
   - Выбери репозиторий `miniAppVictory` → **Import**.
   - **Build Command** и **Output Directory** оставь как есть (проект статический, билд не нужен).
   - Нажми **Deploy**.

3. **Готово**
   - Через минуту появится ссылка вида `https://mini-app-victory-xxx.vercel.app`.
   - Эту ссылку укажи в BotFather как URL Mini App.

После деплоя подставь полученный HTTPS-URL в настройки Mini App в BotFather.

## Структура проекта

```
telegram-quiz-miniapp/
├── index.html      # разметка и экраны
├── css/
│   └── style.css   # стили
├── js/
│   ├── app.js      # логика приложения и Telegram Web App
│   └── quiz-data.js # категории и вопросы
└── README.md
```

Удачи с викториной! 🚀
