# 🎯 Telegram Mini App — Викторина

Мини-приложение для Telegram с викторинами: игра один или с друзьями в комнатах, чат, много вопросов и отдельная тема **Valorant**.

## Что внутри

- **Один** — выбор категории и прохождение викторины solo.
- **С друзьями** — создание комнаты по коду, вход по коду, лобби с участниками и **чатом**, старт игры ведущим, общая викторина и таблица лидеров в конце.
- **Категории:** Общие знания, Наука, История, **Valorant** (агенты, карты, оружие, способности и т.д.) — в каждой по 20–50 вопросов.
- Результаты одиночной игры можно отправить боту через `sendData`.
- Поддержка тёмной и светлой темы Telegram.

## Комнаты и чат (Supabase)

Чтобы работали «С друзьями», нужен [Supabase](https://supabase.com) (бесплатный тариф).

### 1. Создай проект в Supabase

1. Зайди на [supabase.com](https://supabase.com) → **New Project**.
2. Запомни **Project URL** и **anon public** key (Settings → API).

### 2. Создай таблицы

В Supabase: **SQL Editor** → New query → вставь содержимое файла **`supabase/schema.sql`** → Run.

### 3. Включи Realtime

**Database** → **Replication** → включи репликацию для таблиц:  
`rooms`, `room_players`, `room_messages`, `room_answers`.

### 4. Подставь ключи в приложение

В файле **`js/supabase-config.js`** укажи:

```js
const SUPABASE_URL = 'https://ТВОЙ_ПРОЕКТ.supabase.co';
const SUPABASE_ANON_KEY = 'твой_anon_key';
```

После деплоя на Vercel эти значения будут в коде (для учебного проекта подойдёт; для продакшена можно вынести в env и собирать через билд).

Если **не** настроить Supabase, вкладка «С друзьями» скрывается, работает только одиночная игра.

## Как запустить локально

```bash
cd telegram-quiz-miniapp
python3 -m http.server 3000
```

Открой в браузере: `http://localhost:3000`.

## Как подключить к боту в Telegram

1. Создай бота в [@BotFather](https://t.me/BotFather): `/newbot`.
2. **Bot Settings** → **Menu Button** / **Configure Mini App** → укажи URL твоего приложения (например `https://mini-app-victory.vercel.app`).

## Добавление вопросов

В **`js/quiz-data.js`**:

- Новая категория: добавь объект в `QUIZ_DATA` с полями `name`, `emoji`, `questions`.
- Вопрос: `{ question: '...', answers: ['a','b','c','d'], correct: 0 }` (correct — индекс правильного ответа 0–3).

## Деплой на Vercel

1. Залить код на GitHub (репозиторий [miniAppVictory](https://github.com/JLNap/miniAppVictory)).
2. В [Vercel](https://vercel.com): **Add New** → **Project** → Import репозитория → **Deploy**.
3. URL вида `https://mini-app-victory-xxx.vercel.app` указать в BotFather как Mini App.

## Структура проекта

```
telegram-quiz-miniapp/
├── index.html
├── css/style.css
├── js/
│   ├── app.js           # логика: соло, комнаты, чат, мультиплеер
│   ├── quiz-data.js     # категории и все вопросы
│   └── supabase-config.js  # URL и anon key Supabase
├── supabase/
│   └── schema.sql       # таблицы rooms, room_players, room_answers, room_messages
├── vercel.json
└── README.md
```

Удачи с викториной! 🚀
